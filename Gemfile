# frozen_string_literal: true

source 'https://rubygems.org'

# Specify your gem's dependencies in sql_query_analyzer.gemspec
gemspec

gem 'rake', '~> 13.0'

gem 'rspec', '~> 3.0'

gem 'rubocop', '~> 1.21'

gem 'pastel', '~> 0.8.0'
gem 'rails', '>= 6.0', '< 8.0'
group :development, :test do
  gem 'pry-rails'
  gem 'simplecov', require: false
  gem 'sqlite3'
end
